export function startSessionCleanup(): void {
  // Session cleanup script is not part of the npm package surface.
  // Keep this hook as a no-op so runtime startup has no external script dependency.
}
