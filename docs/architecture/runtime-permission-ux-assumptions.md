# Assumptions Ledger: Permission Auto-Mode UX + Skill Installs

Companion to `docs/architecture/runtime-permission-ux-goal-prompt.md`. Every
implementation stage records assumptions it made because information was
missing — one row per assumption, under its stage section. The orchestrator
fills `Validated` (`ok` / `fixed` / `escalate`) before the stage commit. A
stage with no assumptions writes "None."

Format:

| #   | Assumption | Missing info that forced it | Choice taken | Impact if wrong | Validated |
| --- | ---------- | --------------------------- | ------------ | --------------- | --------- |

## Stage 0 — Plan decisions (user-approved)

| #   | Assumption                                                              | Missing info that forced it                                                   | Choice taken                                                                                                                                                          | Impact if wrong                                                         | Validated                      |
| --- | ----------------------------------------------------------------------- | ----------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------- | ------------------------------ |
| 0.1 | Relaxing auto mode reverses part of the recorded "trust pause" decision | —                                                                             | User explicitly approved: `auto` consults the classifier for unproven commands; `auto_strict` preserves the strict behavior; denylist/sanitized-input rails unchanged | —                                                                       | ok — user direction 2026-07-15 |
| 0.3 | `auto` = allow-leaning classifier                                       | User challenge 2026-07-15: "how is it a classifier if we still have to allow" | Classifier allows unless it identifies concrete risk; ask is the exception; `yolo` stays the pure denylist-reverse mode; `auto_strict` stays paranoid                 | Riskier gray-zone commands auto-run — bounded by denylist + risk rubric | ok — user direction            |
| 0.2 | These fixes ride the `feature/otel-llm-observability` branch            | User said "fix in the same branch"                                            | One branch, one PR carrying both observability and UX work                                                                                                            | Bigger PR to review                                                     | ok — user direction            |

## Stage A — Skill install reliability

| #   | Assumption                                                                     | Missing info that forced it                                                                                                   | Choice taken                                                                                                                                                                                 | Impact if wrong                                                                    | Validated                      |
| --- | ------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------- | ------------------------------ |
| A.1 | Immediate same-session skill context remains singular for multi-skill installs | `installed_skill_context` supports one skill, and expanding its consumer is outside Stage A scope                             | Receipt wording contract: `Activation: The first installed skill, <name>, is active in this conversation immediately; the remaining installed skills are available from the next message.`   | A later contract may need plural immediate context                                 | ok — user direction 2026-07-15 |
| A.2 | Multi-skill installs use partial-success semantics                             | Atomicity was unresolved and cross-skill rollback would make unrelated valid skills unavailable                               | Each discovered skill uses the existing single-skill install, bind, settings-sync, and per-skill rollback path independently; one failure does not block or roll back other successes        | Users may need to retry only the named failures                                    | ok — user direction 2026-07-15 |
| A.3 | Install-command discovery is bounded                                           | An installer can produce an arbitrarily large skill tree                                                                      | Discover at most 25 nested skill roots in deterministic alphabetical path order, skip the remainder with a receipt note, and load assets only for each selected skill as it is processed     | Skills beyond the cap require another install command                              | ok — user direction 2026-07-15 |
| A.4 | Failure diagnostics can contain credentials in URIs or labelled values         | Existing failure sanitizers did not cover hierarchical URI credentials or complete multi-word labelled values                 | Mask URI credentials and suppress credential-bearing lines before caller redaction; preserve only the five exact tested safe status diagnostics through a positive allowlist                 | A missed credential could leak into a chat receipt                                 |                                |
| A.5 | Verbose command failures usually put the useful reason at the end              | Truncated output has no structured field identifying the actual failure reason                                                | Retain a bounded raw tail, discard its first retained line or fragment when truncated, sanitize the complete remaining lines, and prefix the truncation marker                               | Single-line overflow yields only the marker; earlier useful output may be omitted  |                                |
| A.6 | Complete PEM blocks must be redacted before labelled-value masking             | Review found that line-wise labelled-value masking can remove the PEM `BEGIN` line before shared PEM redaction sees the block | Redact complete PEM blocks across the whole retained text first, then apply line-wise labelled-value masking                                                                                 | Private-key body lines could leak into a failure receipt                           |                                |
| A.7 | Rollback must not overwrite concurrent newer skill state                       | Review found that unconditional snapshot restoration can race with newer catalog entries, bindings, or artifacts              | Compare current state with the catalog entry, binding, or artifact written by this install; restore only matching state, otherwise skip restoration and note the skip in the failure receipt | A failed install could overwrite newer state, or leave its own stale state behind  |                                |
| A.8 | A skill with more than 50 assets is a per-skill failure                        | Review found that asset collection stopped at 50 files without reporting that more files existed                              | Detect overflow, fail that skill with an explicit over-cap receipt reason, and preserve partial success for other skills                                                                     | Oversized skills could install incompletely, or require reducing their asset count |                                |

Autoreview decision note: the legacy single-skill receipt assertion was updated
to the user-approved A.1 `Installed: <names>` contract rather than reverting the
approved wording.

Autoreview safety note: ordinary package failures remain independent. If the
durable settings authority cannot be reconciled after a failed package, the
receipt preserves earlier successes and processing stops before another package
can widen the inconsistent state.

### Stage A orchestrator notes

- Two Codex sessions were lost to tooling issues during this stage (a wedged in-session autoreview, then a read-only sandbox); the orchestrator applied the final orphan-PEM tail hardening directly (A.9) after Codex's adjudication confirmed all findings real.

| #   | Assumption                                       | Missing info that forced it                                                   | Choice taken                                                                                                              | Impact if wrong                            | Validated                               |
| --- | ------------------------------------------------ | ----------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------ | --------------------------------------- |
| A.9 | Tail truncation can orphan a PEM block's markers | Review r2: the retained window can slice off the BEGIN line (or both markers) | Orphaned END/BEGIN fragments and runs of 3+ long base64-only lines redact in the failure sanitizer; regression test added | Marker-less key bodies could relay to chat | ok — orchestrator-applied, test-covered |

### Stage A review round 3 (orchestrator-applied)

| #    | Assumption                                                                                  | Missing info that forced it                                                                                                                                         | Choice taken                                                                                                                                                                                 | Impact if wrong                                             | Validated |
| ---- | ------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------- | --------- |
| A.10 | Bundle fingerprints must ignore store-inferred fields                                       | Review r3 (P1): stores infer `contentType` on read; fresh assets leave it undefined — every real-store compare differed, so rollback always skipped as "concurrent" | Fingerprint hashes path+content only                                                                                                                                                         | Failed replacements stay active with a failure receipt      | fixed     |
| A.11 | Compare-and-restore must be atomic vs concurrent installs                                   | Review r3 (P2): check-then-write window                                                                                                                             | In-process keyed mutex (`withSkillMaterializationLock`) serializes install writes and rollback compare+restore per materialization key — sufficient for the single-host-process install path | Stale snapshot could clobber a concurrent same-name install | fixed     |
| A.12 | The fresh-install cleanup block moved to `rollbackFreshInstallBinding` in the assets helper | Handlers file crossed its 700-line budget                                                                                                                           | Pure extraction, behavior preserved (existing tests cover both cleanup paths)                                                                                                                | —                                                           | ok        |

| A.13 | Rollback baselines must live in the store's own representation | Review r4 (P1): Postgres timestamptz formatting differs from `nowIso`, so in-memory rows never deep-equal reread rows — rollback always skipped on production PG | After bind, the persisted skill+binding are reread and used as the expected state for compare-before-restore | Failed replacements stay active on real Postgres | fixed |
| A.14 | The critical section spans snapshot → install → bind → persisted reread → settings sync | Review r4 (P1): snapshot-before-lock let two same-key installs snapshot the same old version and cross-clobber | Entire sequence runs inside `withSkillMaterializationLock`; rollback acquires the same key lock | Stale receipts / stale-snapshot restores under same-name concurrency | fixed |

| A.15 | `installSkill` can fail after overwriting the artifact but before the catalog save | Review r5 (P1) | No-installedSkill rollback path heals partial writes: if the catalog row still matches the snapshot but the stored artifact diverged, the snapshot bundle is restored (artifact only) | Old catalog hash pointing at a new bundle breaks the previously working skill | fixed |
| A.16 | The materialization lock covers only command-based installs in this process | Review r5 (P1) wants durable CAS shared by ALL writers (Control API, package installs, other processes) | Held: Stage A's surface is the chat-driven install path on the single-host runtime; durable versioned artifact CAS is the named follow-up | Cross-path same-name concurrent installs within the compare-restore window could still clobber | ok — documented residual, revisit: repository-level versioned CAS |
| A.17 | Stopping early must name every unattempted root | Review r5 (P2): `break` silently abandoned the remainder | `reportUnattemptedSkillRoots` adds each to the receipt's Failed list | Users unaware which skills still need installing | fixed |

| A.18 | YAML block-scalar credentials hide on continuation lines | Review r6 (P1): `password: \|` left the secret on more-indented lines | `redactLabelledContinuationBlocks` drops a credential label's block continuation lines entirely | Multiline secrets relayed to chat | fixed |
| A.19 | Rollback's artifact-then-catalog write order can desync on `saveSkill` failure | Review r6 (P2) | Compensating rewrite: on `saveSkill` failure the replacement bundle is re-put so catalog hash and stored bytes stay consistent, then the restore failure reports | Integrity-check failures on active bindings | fixed |
| A.20 | Recovered installs must count for duplicate detection | Review r6 (P2): keep-as-installed path skipped `installedMaterializationKeys` | Key recorded before continuing | Same-name later root overwrites the recovered skill with stale receipt context | fixed |

| A.21 | Every in-process skill writer shares the materialization lock | Review r7 (P1, sharpening A.16): permission-review installs and the Control API upload also write skills | Lock moved to `apps/core/src/shared/skill-install-lock.ts` (layer-legal for all writers); acquired at all three write seams and in rollback. Cross-PROCESS coordination remains the A.16 named follow-up | In-process same-name races closed; fleet multi-process window documented | fixed (in-process) |
| A.22 | Credential labels can introduce bracketed structures, not just YAML scalars | Review r7 (P1): `passwords: [\n "hunter2"\n]` leaked the continuation line | Block redaction also triggers on `[`, `{`, `(` openers | Structured multiline secrets relayed to chat | fixed |

| A.23 | Indentationless YAML sequences continue a credential block | Review r8 (P1): `passwords:\n- hunter2` — sequence items at the label's own indent escaped the continuation check | Continuation also covers same-indent lines starting `- ` | Listed secrets relayed to chat | fixed |
| A.24 | Fresh-install cleanup must re-verify state under the lock | Review r8 (P1): between a failed install and its cleanup, a queued same-name writer may have replaced the binding | Cleanup runs under the keyed lock with a `confirmState` deep-equal check against this attempt's binding; mismatch → skip with an explicit receipt line | Cleanup would unbind a newer writer's healthy skill | fixed |
| A.25 | Permission-review installs hold the lock across the full sequence | Review r8 (P1): lock covered only `installSkill`; bind, sync, and catch-side rollback ran outside it | Whole install→bind→sync→compensation block is one critical section (ordering proven by test) | Concurrent writer interleaves with a partial review install | fixed |

| A.26 | Truncation can slice off a credential label, stranding its continuation lines | Review r9 (P1): a 4 KB tail starting inside a `passwords:` block leaves label-less `- secret` lines no redactor recognizes | After dropping the partial first line, leading continuation-shaped lines (blank, indented, `- ` items) are dropped until the first fresh statement | Listed secrets relayed to chat from truncated diagnostics | fixed |
| A.27 | Command-install compensation must run inside the original lock, not a re-acquired one | Review r9 (P1): releasing the lock on failure lets a queued same-name install snapshot the failed intermediate state as its "previous" state | The whole snapshot→install→bind→sync→compensation sequence is one locked callback returning an outcome object; the loop acts on it outside the lock | Second writer restores a broken state instead of the original | fixed |
| A.28 | Partial-write heal must restore even when the broken artifact is unreadable | Review r9 (P1): stores replace artifacts in place; a failed write can leave a bundle `getSkillArtifact` cannot decode, and the heal bailed without rewriting the snapshot | Unreadable current artifact is treated as diverged (fingerprint `undefined`) so the snapshot rewrite still runs when the catalog row matches | Catalog left pointing at a corrupt bundle | fixed |
| A.29 | The materialization-lock registry must not grow unboundedly | Review r9 (P2): keys are request-derived; settled entries lingered for the process lifetime | Entry deleted after its sentinel settles, guarded by an identity check so a queued writer's newer sentinel is never removed | Slow memory growth in long-running runtimes | fixed |

| A.30 | In-lock compensation must restore, never skip on divergence (supersedes A.24's skip semantics) | Review r10 (P1×3, one root cause): with compensation inside the attempt's lock, deep-equal baselines are mutated by the attempt's OWN writes — installSkill's durable row before a later throw, sync reconciliation touching bindings, driver read-normalization — so "state changed → concurrent → skip" false-triggers and leaves broken state active | `rollbackInstalledSkillReplacement` is now compare-then-RESTORE: any divergence from the snapshot (row, binding, or bundle fingerprint, unreadable counts as diverged) rewrites the snapshot; `rollbackFreshInstallBinding` unbinds unconditionally (confirmState removed). In-process a held lock proves divergence is self-inflicted; cross-process stays the A.16 follow-up | Failed installs reported as failed while replacement state silently stayed active | fixed |
| A.31 | Compensation baselines must come from persisted reads, not in-memory returns | Review r10 (P1): bind-failure rollback compared `installSkill()`'s return against a storage re-read; drivers can normalize fields on read | The persisted row is re-read immediately after install, before binding | Restore skipped on representation-only differences | fixed |
| A.32 | The saveSkill-failure compensating rewrite survives the restore redesign (A.19 carried forward) | r10 redesign made the attempted bundle an explicit `attemptedAssets` input | On saveSkill failure with a diverged row, the attempted bundle is re-put so catalog hash and stored bytes stay consistent | Integrity mismatch on active bindings | fixed |
| A.33 | Compound credential labels contain spaces | Review r10 (P1): `database password: \|` missed the block redactor; only the label tail was redacted, continuation secret relayed | Label prefix/suffix accept spaces and tabs in `CREDENTIAL_LABEL_LINE_PATTERN` | Multiline secrets relayed from spaced labels | fixed |
| A.34 | An exact newline-boundary truncation keeps its first complete line | Review r10 (P2): the first tail line was dropped unconditionally; when the retained extra byte is `\n` the line is already complete and may be the only diagnostic | First-line drop now conditional on the boundary byte; label-less continuation dropping still runs after | Sole failure reason reduced to `[REDACTED_TRUNCATED_OUTPUT]` | fixed |
| A.35 | Headless installers must be told they run under an agent | Live smoke (2026-07-15): `npx skills add …` exited 0 in ~2s writing nothing — the CLI fell back to interactive prompts because `safeInstallerEnv` strips the agent markers its detect-agent check needs; `CI=1` is ignored by that flow | `safeInstallerEnv` sets the vendor-neutral `AI_AGENT=1`; verified end-to-end through the real runner (root discovered at `.agents/skills/…`) | Every `skills`-CLI install no-ops with "Installer command did not produce a SKILL.md file." | fixed |
| A.36 | Desired-state export must tolerate bindings whose conversation lost its provider connection | Live smoke (2026-07-15): installs that got past discovery crashed in `syncApprovedCapabilitySettings` → `exportCurrentDesiredState` with `Cannot read properties of undefined (reading 'installedAgents')` — the bindings loop can resolve a conversation id through the settings fallback that the first loop never exported (connection gone); the rejection surfaces only to the agent, nothing is logged | The binding is skipped together with its conversation (matching the first loop's drop-connectionless intent); regression test with an absent provider account | Every capability sync crashes, so no skill/tool install can complete | fixed |
| A.37 | Desired-state export must not strip settings-owned config the DB never projected | Live smoke (2026-07-15): past the A.36 crash, capability syncs failed full-file validation — the export rebuilt provider accounts with `runtime_secret_refs: {}` and conversations with `control_approvers: []` although the live settings.yaml carries both (env-ref token setups and hand-configured approvers have no DB rows) | Empty DB projections fall back to the existing settings entry (stored rows stay authoritative when present, per the desired-state-service precedence test); regression test with empty DB refs/approver rows | Every capability sync fails validation, so no skill/tool install can persist | fixed |
| A.38 | Oversized permission full views belong in the group, next to the prompt | User decision (2026-07-15): the `permission-payload.txt` DM confused the approver — the prompt is already visible in the group, so a private copy adds no privacy and strands context | Telegram full-view documents now go to the originating chat + thread with a "Full details for: …" caption; the approver-DM routing helper is deleted, prompt line reads "sent above for review." | Approvers hunt for an uncontextualized DM attachment | fixed |
| A.39 | Canonical fallback `channel-…` account rows are storage plumbing, never config | Live smoke (2026-07-15): `ensureConversation` mints `channel-providerAccount:default:<provider>` rows (with phantom agents) when ingestion lacks an attributed account; exporting them produced enabled telegram/slack accounts with no secret refs → validation failure. Rows must stay in the DB (messages reference them) | `exportCurrentDesiredState` filters them like internal app/control accounts; regression test | Every capability sync fails validation on phantom accounts | fixed |
| A.40 | Only operating surfaces belong in the settings export | Live dry-run (2026-07-15): observed-but-unconfigured chats (a group with no installs, a channel with only disabled installs) and composite live-turn queue jids (`<chat>::agent:…::provider_account:…`) leaked into the export as conversations with empty `control_approvers` → validation failure | Stored conversations export only with a settings entry, approver rows, or an active install; group routes with composite jids are skipped (the base chat exports through the other paths). Verified read-only against the live DB: validation ok | Unconfigured observed chats block every capability write | fixed |
| A.41 | Capability syncs must survive a stale settings base | Agent analysis, validated (2026-07-15): `restart-sync.ts` loaded the file base once while echo revisions (`settings.yaml:auto-import`) and `permission:persistent-tool-rule` writes keep advancing the DB head, so `SettingsStaleMutationError` hard-failed every install/approval sync, and the rollback's own sync failed the same way ("Failed …/Restore failed …" pairs). Live-verified: installs reached catalog+artifact+binding, then were unbound by the failing sync | Bounded reload+re-export+retry (3 attempts, stale/conflict errors only) in `syncRuntimeSettingsFromProjection` and `addAgentToolRulesToSyncedRuntimeSettings`, ported from the onboarding pattern (Codex-implemented); repro test proves retry-then-succeed and no-retry-on-real-errors. Stale-check semantics untouched | Any concurrent revision writer permanently breaks capability writes | fixed |
| A.42 | Revision documents must be canonical regardless of origin | The identity guard test (r-codex) failed: parse-time defaulting (`kind: group→channel`, `requires_trigger`, `persona`, `agent.default_model`) makes documents built from in-memory settings never equal documents built from parsed files — the source feeding the echo revisions | `settingsToRevisionDocument` canonicalizes through a render→parse round-trip at the choke point, so all four asymmetries (and future ones) collapse; the guard test now asserts identity holds | Every settings write mints an echo revision, keeping the base perpetually stale | fixed |

| A.43 | Async/delegated task tools follow the permission stack, not the sandbox | User decision (2026-07-15, grill-me): worker mode must work with or without an OS sandbox; four `enforcing === true` gates (spawn env flag, IPC `taskService`, `runSandboxedAsyncCommand` throw, recovery stub + queued-recovery skip) made `delegate_task`/async tools unmountable in direct mode despite an AgentDelegation grant | All four gates removed (Codex-implemented): tools mount on async-repo availability (+ AgentDelegation for delegation); direct mode executes background commands like foreground ones via the direct provider's `start()` (egress proxy env injected, `maxProcesses` cap dropped as user-wide RLIMIT_NPROC can't scope one tree); enforcing path unchanged; recovery builds the real service in both modes. No strict-mode knob by decision | Delegation and async tasks permanently unavailable on unsandboxed hosts | fixed |

| A.44 | Canonical revision docs must keep app-workspace account/conversation pairing | Live repro (2026-07-15): the A.42 render→parse canonicalization desynced internal `app`-provider accounts from their conversations; legacy-bindings reconstruction minted `app_default:agent:agent:…` refs and every sync failed validation ("references unknown provider account") — invisible in logs until the FIX-3 logging landed | Codex fixed the round-trip seam so app-provider pairs survive canonicalization; unit test pins it; acceptance = the real `syncRuntimeSettingsFromProjection` against the live DB prints SYNC SUCCEEDED (verified post-restart; the OLD running binary's echo revisions made retries legitimately non-convergent until restart) | Every capability sync fails; multi-skill installs roll back with "prior failure stopped this install" | fixed |
| A.45 | Explicit API clears must not be resurrected by export fallbacks (r12 P1×2) | `PUT approvers []` / provider-account PATCH `runtimeSecretRefs: {}` deleted rows, then the A.37 fallbacks re-copied old settings.yaml values and reconciliation re-inserted them — revoked authority revived | Explicit-clear markers (e.g. `__gantryRuntimeRefsAuthoritative`) make DB writes authoritative; file-only legacy setups still export (A.37 preserved); tests cover clear-then-export and file-only halves | Revoked approver/credential silently reauthorized | fixed |
| A.46 | Round-12 adjudication of remaining findings | P1 "keep async commands sandboxed" REJECTED — contradicts the explicit A.43 user decision; foreground direct-mode commands run equally unconfined, so no asymmetry. P2 cross-process restore clobber HELD — single-process hosts safe under the in-process lock; fleet-grade durable CAS remains the named A.16 follow-up | Documented here; no code change | — | held/rejected by decision |

## Stage B — Auto mode that decides

| #    | Assumption                                                                                             | Missing info that forced it                                                                                                                                                                                                  | Choice taken                                                                                                                                                                                 | Impact if wrong                                                                                        | Validated |
| ---- | ------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------ | --------- |
| B.1  | “Workspace + granted folders” uses the existing workspace root and approved capability boundary        | The deterministic gate receives no separate granted-folder root list, and adding new authority plumbing is outside Stage B                                                                                                   | Preserve realpath confinement to `workspaceRoot` and require the existing matching approved capability; do not invent folder authority                                                       | A future explicit granted-root projection must extend the gate input and confinement check             |           |
| B.2  | “Recently approved” mirrors the existing seven-day recent-denial window                                | The contract sets the N≥2 threshold but does not define recency duration or approval/denial conflict precedence                                                                                                              | Set the boolean when `allowCount >= 2` and `updatedAt` is within seven days, unless a recent denial exists                                                                                   | A different duration or precedence changes classifier context, not durable authority                   |           |
| B.3  | The first `decisionOptions` entry is the current prompt’s primary/default action                       | The interaction contract has ordered options but no separate primary/default field                                                                                                                                           | At N≥2, reorder the existing persistent option ahead of allow-once in both IPC and inline callback seams                                                                                     | An adapter that ignores option order would need an explicit primary-action field                       |           |
| B.4  | The bounded “permission callback” scope follows the repository’s actual callback seams                 | The goal names `apps/core/src/runtime/permissions/`, but the mode and promotion callbacks live in runtime IPC, inline bootstrap, runner, and application permission modules                                                  | Touch only the existing seams needed to carry `auto_strict` and reorder the promotion action; add no new callback abstraction                                                                | A literal directory-only interpretation would leave one runtime lane or runner mode inconsistent       |           |
| B.5  | Existing public settings schemas must accept the same additive mode vocabulary as the settings parsers | Expanding the shared `PermissionMode` exposed compile mismatches in the existing settings response schemas                                                                                                                   | Add `auto_strict` to the existing permission-mode enums and schema tests without changing endpoint shapes                                                                                    | If public vocabulary were intentionally frozen, `auto_strict` would need a separate private projection |           |
| B.6  | Stage B2’s missing bounded scope is the sandbox adapter seam plus focused tests                        | Stage B2 gives behavior and acceptance criteria but no file list; the Gantry egress gateway on this branch already default-allows public hosts, while sandbox-runtime still gates undeclared domains before the parent proxy | Use the sandbox-runtime library callback in a tiny same-adapter runner so unmatched public destinations reach the Gantry parent proxy; keep the parent proxy as denylist and audit authority | A future sandbox-runtime default-allow configuration option could replace the wrapper                  |           |
| B.7  | Sandbox Runtime unmatched-destination approval must reject direct local-network targets                | Review P1: Sandbox Runtime dials loopback and private destinations directly instead of through the Gantry parent proxy                                                                                                       | Keep unmatched public destinations allowed, but reject localhost plus literal loopback, link-local, and private addresses in the callback                                                    | Local targets bypass Gantry egress private-address checks, denylist enforcement, and CONNECT audit     | fixed     |
| B.8  | Git commands are not deterministic silent reads                                                        | Review P1: workspace-owned Git config can execute fsmonitor, diff, and textconv helpers, while revision selectors can address hidden files                                                                                   | Remove every Git command from the deterministic silent-allow set and leave it to the Stage B classifier                                                                                      | Silent Git execution can run arbitrary helpers or expose hidden workspace content                      | fixed     |
| B.9  | Conversation permission commands must expose the strict auto mode                                      | Review follow-up: the shared mode vocabulary includes `auto_strict`, but `/permissions` parsing and help omitted it                                                                                                          | Accept, document, and test `/permissions auto_strict` alongside the existing conversation modes                                                                                              | Users cannot switch an individual conversation back to strict classifier behavior                      | fixed     |
| B.10 | Recursive grep is not a deterministic silent read                                                      | Review P1: validating only the explicit root still lets recursive grep traverse hidden descendants                                                                                                                           | Reject every recursive grep form from the deterministic proof set and leave it to the allow-leaning classifier                                                                               | Hidden credential files under an approved root could be read silently                                  | fixed     |
| B.11 | Unmodelled leading find options invalidate deterministic proof                                         | Review P1: `find -O1 /etc` and `find -f /etc` defaulted proof to workspace `.` while the command traversed `/etc`                                                                                                            | Reject any unmodelled option or expression before the first plain find root                                                                                                                  | Find could traverse an unvalidated root silently                                                       | fixed     |
| B.12 | Find `-follow` is a symlink-following traversal form                                                   | Review P2: the traversal denylist covered `-H`/`-L` but omitted the equivalent `-follow` expression                                                                                                                          | Reject `-follow` with the existing symlink-following find forms                                                                                                                              | Traversal could escape through a symlink after root validation                                         | fixed     |
| B.13 | Promotion ordering must synthesize default options                                                     | Review P2: interactive and DeepAgents IPC requests omit `decisionOptions`, so the N≥2 reorder never ran                                                                                                                      | Derive the default options from valid persistent suggestions when options are absent, then promote `Allow for future`                                                                        | Repeated approvals keep `Allow once` primary in production                                             | fixed     |
| B.14 | Active permission docs must describe both auto postures                                                | Review P2: capability guidance still promised that unprovable calls always ask and the classifier never widens the deterministic floor                                                                                       | Document Stage B's user-approved allow-leaning `auto` behavior and deterministic-proof-only `auto_strict`                                                                                    | Operators choose a mode using a false safety contract                                                  | fixed     |
| B.15 | Grep's `-d ACTION` consumes a value and can select recursive traversal                                 | Review P1: `grep -d recurse . .` consumed `recurse` as the pattern while the gate validated only the following roots                                                                                                         | Remove `d` from the no-value whitelist and reject `-d` forms from deterministic proof                                                                                                        | Grep could silently read hidden descendant contents under a validated root                             | fixed     |
| B.16 | Operand-less `df` has no workspace-local default                                                       | Review P2: the gate validated synthetic `.` while `df` actually reported every mounted filesystem                                                                                                                            | Require at least one validated `df` operand; only `du` retains the workspace default                                                                                                         | Host filesystem metadata could be exposed outside the validated boundary                               | fixed     |
| B.17 | Deterministic-proof docs must match the reviewed command set and traversal limits                      | Review P3: active guidance still described an `ls`/`cat`-only gate and incorrectly excluded all hidden-entry behavior                                                                                                        | Enumerate every reviewed shell command and document operand, recursive, hidden-descendant, symlink, and confinement restrictions                                                             | Operators cannot accurately evaluate the `auto_strict` safety boundary                                 | fixed     |
| B.18 | Recursive-by-default searchers cannot prove non-secret reads                                           | Review P1: `rg <pattern> <dir>` validates only the explicit directory while ripgrep opens every descendant, including secret files                                                                                           | Remove ripgrep and other recursive-by-default searchers such as `find` from deterministic proof; leave them to the allow-leaning classifier                                                  | Hidden descendant content could be read silently                                                       | fixed     |
| B.19 | Only real human approvals are operator-approval evidence                                               | Review P2: classifier auto-allows incremented the same counter used to claim that the operator recently approved the exact shape                                                                                             | Stop classifier auto-allows from incrementing the counter; only real approver decisions can produce `recentlyApprovedExactToolShape`                                                         | Automatic decisions could recursively justify later automatic decisions                                | fixed     |
| B.20 | Unmatched sandbox destinations require a Gantry parent proxy                                           | Review P2: the permissive callback was installed for no-network profiles, and hostname-based private destinations bypassed its literal-IP check                                                                              | Install the callback only when a Gantry parent proxy is configured; otherwise omit it so Sandbox Runtime denies unmatched destinations                                                       | No-proxy profiles could connect directly to public or hostname-hidden private destinations             | fixed     |
| B.21 | `file -f NAMEFILE` cannot prove that every opened target is safe                                       | Review P2: the gate validated only `NAMEFILE`, while `file` opened every target path listed inside it                                                                                                                        | Remove `f` from `file`'s deterministic option allowlist so every `-f` form remains unproven                                                                                                  | Hidden, protected, or out-of-workspace targets could be read silently                                  | fixed     |
| B.22 | Human approval and denial signals require explicit human attribution                                   | Review P2: Discord timeout and disconnect cancellations omitted `decidedBy` but were recorded as human denials                                                                                                               | Do not record approval or denial shape signals when `decidedBy` is absent or system-generated                                                                                                | System cancellations could poison `recentlyDeniedExactToolShape`                                       | fixed     |
| B.23 | Active deterministic-proof docs must exclude `rg` and `find`                                           | Review P3: capability guidance still listed both commands after B.18 and while implementation tests asserted they were unproven                                                                                              | Remove both commands and their deterministic traversal claims from the active command set                                                                                                    | Operators could rely on a safety contract the implementation does not provide                          | fixed     |
| B.24 | Active promotion docs must match the human-attributed inline prompt flow                               | Review P2: capability guidance still described classifier auto-allow counting, a threshold of three, a separate promotion offer, and omitted `auto_strict` from `/permissions`                                               | Document human-attributed approvals only, threshold two, `Allow for future` on the normal prompt, no separate offer flow, and the complete `/permissions` mode vocabulary                    | Operators could misunderstand which decisions count and expect a prompt flow that no longer exists     | fixed     |
| B.25 | The callerless classifier-auto-allow promotion scheduler is obsolete                                   | Review P3 and a repository-wide symbol search found `schedulePermissionPromotion` and `processPermissionPromotion` only in their definitions and old-flow unit tests                                                         | Delete the dead scheduler/process path and its obsolete tests; retain the counter threshold and prompt-facing types used by live prompting                                                   | Dead code and tests could preserve the false separate-offer model and invite its accidental reuse      | fixed     |

## Stage C — Prompt UX

_(Codex fills this in.)_

## Stage D — Stream boundary after prompts

_(Codex fills this in.)_
